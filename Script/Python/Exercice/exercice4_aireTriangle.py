import math

cone_ray =  input("Pouvez vous me donner le rayon de votre triangle : ")
cone_height =  input("Pouvez vous me donner la hauteur de votre triangle : ")

transform_int_cone_ray = float(triangle_ray)
transform_int_cone_height = float(triangle_height)

print(f"Le volume du cône droit est : ", transform_int_triangle_height, "cm, et le rayon de ", transform_int_triangle_height, "cm est égale à : ", math.pi * transform_int_triangle_ray**2 * transform_int_triangle_height / 3, "cm3") 